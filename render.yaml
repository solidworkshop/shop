
services:
  - type: web
    name: shop-capi-pixel
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k gthread -w 1 -b 0.0.0.0:$PORT app:app
    healthCheckPath: /healthz
